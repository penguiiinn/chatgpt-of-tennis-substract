- [ ] Update `aceintel/app.js` autocomplete fetch logic:
  - [ ] Add AbortController to cancel old requests
  - [ ] Add try/catch/finally around fetch
  - [ ] Ensure “Searching…” loader is removed in finally
  - [ ] Ensure `response.json()` is awaited
  - [ ] Add `console.log(data)` after API response
  - [ ] Render dropdown when `data.length > 0`
  - [ ] Show “No players found” when `data.length === 0`
  - [ ] On fetch failure: clear loader and log error
- [ ] Test autocomplete locally with rapid typing (e.g., “anna”)
- [ ] Push changes

